name: Validate immutable template snapshot

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]

permissions:
  contents: write

concurrency:
  group: agent-template-snapshot-race
  cancel-in-progress: true

jobs:
  validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
        with:
          ref: agent/fix-template-snapshot-race
          fetch-depth: 0
      - name: Apply template snapshot patch
        run: python .github/patches/agent-template-snapshot.py
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy
      - name: Install Linux Tauri compile dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf
      - run: cargo fmt --all
      - name: Run template snapshot regression tests
        run: cargo test -p dokkomplekt-tauri template_snapshot -- --nocapture
      - name: Compile Tauri tests
        run: cargo check -p dokkomplekt-tauri --tests
      - name: Clippy Tauri tests
        run: cargo clippy -p dokkomplekt-tauri --tests -- -D warnings
      - name: Run template and governance contracts
        run: python -m pytest -q tests/test_v18_4_5_installed_button_creation.py tests/test_v18_4_3_generation_plan_governance.py
      - run: git diff --check
      - name: Commit validated fix
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/patches/agent-template-snapshot.py .github/workflows/agent-template-snapshot.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Fix template snapshot TOCTOU"
          git fetch origin agent/fix-template-snapshot-race
          git rebase origin/agent/fix-template-snapshot-race
          git push origin HEAD:agent/fix-template-snapshot-race
