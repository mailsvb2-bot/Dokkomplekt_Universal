# Autopilot evidence directory

Tracked source in this directory defines the capability registry only. Generated `coverage-report.*` and `FULL_AUTOPILOT_REPORT.*` files are CI evidence and must not be committed; GitHub Actions uploads them as run artifacts.
