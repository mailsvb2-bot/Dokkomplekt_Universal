Dokkomplekt Universal 18.4.3 — REPAIRED SOURCE CHECKPOINT, NOT A SIGNED PRODUCTION RELEASE
Repair date: 2026-07-26

Verified on the repaired source tree in the available environment:
- Python pytest: 212 passed, 0 failed.
- Isolated Python contract runner: 33 modules, 212 passed, source unchanged during run.
- Static source quality gate: passed.
- DOCX structural/visual goldens: 7 fixtures passed.
- Production Rust panic shortcut audit: passed (source scan).
- Security backport policy check: passed.
- Starter content-pack verification: passed.
- Reference-data freshness check: passed.
- Source archive builder: deterministic manifest, CRC, safe paths and SHA-256 verification.

Corrected in 18.4.3:
- click launcher now starts a prebuilt EXE when present or the real Tauri dev command with diagnostics;
- all active version/toolchain markers are synchronized;
- the full pytest suite replaces partial unittest discovery in CI and installer checks;
- diary output requires and renders treating-physician and department-head signature lines;
- generated-document number/date are requested fresh rather than inherited from the source;
- trivial repeated-digit INNs are rejected;
- excluded license-server and Python binding crates are compiled, linted, tested and audited in an isolated mandatory Rust gate whose evidence is bound into release attestation;
- current source manifest is regenerated by the deterministic archive builder.

Not independently executed in this repair environment:
- Rust fmt/check/clippy/test/audit (Rust/Cargo toolchain unavailable here);
- TypeScript/Vitest/Vite (npm registry installation did not complete in the environment);
- signed NSIS/AuthentiCode and offline sidecar packaging;
- clean Windows reboot, licensed Word COM, real printer/spooler and watcher evidence;
- measured accuracy on real anonymized professional corpora.

Release remains fail-closed until the mandatory CI, signed release evidence, production sidecars and Windows hardware gates all pass.
