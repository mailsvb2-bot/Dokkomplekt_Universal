{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for local document constructor",
  "windows": ["main"],
  "permissions": ["core:default"]
}
