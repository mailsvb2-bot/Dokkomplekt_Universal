name: One-time donor medical source parity

on:
  push:
    branches:
      - agent/donor-medical-source-parity-20260817

permissions:
  contents: write

concurrency:
  group: donor-medical-source-parity-${{ github.ref }}
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955
        with:
          ref: agent/donor-medical-source-parity-20260817
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: 1.97.1
          components: rustfmt, clippy
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Port proven donor medical source behavior into Rust core
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import json

          def replace_once(path: str, old: str, new: str) -> None:
              p = Path(path)
              text = p.read_text(encoding="utf-8")
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected one replacement anchor, found {count}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1), encoding="utf-8")

          # 1) Restore the donor parser's clinical source breadth and compact one-line behavior.
          source_path = Path("crates/dokkomplekt-core/src/source_parser.rs")
          source = source_path.read_text(encoding="utf-8")
          source = source.replace(
              '        "дата поступления",\n    ]',
              '        "дата поступления",\n        "жалобы",\n        "анамнез",\n        "соматический статус",\n        "профильный статус",\n        "лаборатор",\n    ]',
              1,
          )

          rules_start = source.index("fn medical_rules() -> Vec<LabelRule> {")
          rules_end = source.index("\nfn mirror_medical_to_generic", rules_start)
          medical_rules = r'''fn medical_rules() -> Vec<LabelRule> {
    vec![
        LabelRule {
            field: "medical.case_number",
            labels: &[
                "История болезни №",
                "История болезни N",
                "Номер истории болезни",
                "ИБ №",
                "и/б №",
                "Nr historii choroby",
                "Numer historii choroby",
                "Historia choroby nr",
            ],
            multiline: false,
        },
        LabelRule {
            field: "subject.name",
            labels: &[
                "Ф.И.О.",
                "Ф.И.О",
                "ФИО пациента",
                "Ф.И.О. пациента",
                "Фамилия Имя Отчество",
                "Пациент",
                "Пациентка",
                "Pacjent",
                "Pacjentka",
                "Imię i nazwisko",
                "Imie i nazwisko",
            ],
            multiline: false,
        },
        LabelRule {
            field: "subject.age",
            labels: &["Возраст", "Wiek"],
            multiline: false,
        },
        LabelRule {
            field: "subject.birth_date",
            labels: &["Дата рождения", "Data urodzenia"],
            multiline: false,
        },
        LabelRule {
            field: "subject.address",
            labels: &[
                "Зарегистрирован по адресу",
                "Адрес регистрации",
                "Адрес проживания",
                "Место жительства",
                "Adres zamieszkania",
                "Miejsce zamieszkania",
            ],
            multiline: false,
        },
        LabelRule {
            field: "medical.admission_date",
            labels: &[
                "Дата поступления",
                "Дата госпитализации",
                "Data przyjęcia",
                "Data przyjecia",
                "Data hospitalizacji",
            ],
            multiline: false,
        },
        LabelRule {
            field: "medical.discharge_date",
            labels: &["Дата выписки", "Data wypisu"],
            multiline: false,
        },
        LabelRule {
            field: "medical.complaints",
            labels: &[
                "Жалобы на момент осмотра",
                "Жалобы при поступлении",
                "Жалобы",
                "Skargi przy przyjęciu",
                "Skargi przy przyjeciu",
                "Dolegliwości",
                "Dolegliwosci",
                "Skargi",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.anamnesis_life",
            labels: &["Анамнез жизни", "Wywiad życiowy", "Wywiad zyciowy", "Wywiad osobniczy"],
            multiline: true,
        },
        LabelRule {
            field: "medical.anamnesis_disease",
            labels: &[
                "Анамнез заболевания",
                "Wywiad chorobowy",
                "Wywiad obecnej choroby",
                "Historia choroby",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.profile_status",
            labels: &[
                "Профильный статус при поступлении",
                "Профильный статус",
                "Психический статус при поступлении",
                "Психический статус",
                "Stan psychiczny",
                "Badanie psychiatryczne",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.somatic_status",
            labels: &[
                "Сомато-неврологический статус",
                "Соматический статус",
                "Объективный статус",
                "Объективно",
                "Status praesens",
                "Stan przedmiotowy",
                "Badanie przedmiotowe",
                "Stan somatyczny",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.examination_plan",
            labels: &["План обследования", "Plan badań", "Plan badan"],
            multiline: true,
        },
        LabelRule {
            field: "medical.diagnosis",
            labels: &[
                "Клинический диагноз",
                "Предварительный диагноз",
                "Основной диагноз",
                "Заключительный диагноз",
                "Диагноз",
                "Rozpoznanie kliniczne",
                "Rozpoznanie główne",
                "Rozpoznanie glowne",
                "Rozpoznanie",
                "Diagnoza",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.icd10",
            labels: &["Код МКБ-10", "Код МКБ", "МКБ-10", "ICD-10"],
            multiline: false,
        },
        LabelRule {
            field: "medical.treatment",
            labels: &[
                "План лечения",
                "Назначенное лечение",
                "Лечение",
                "Plan leczenia",
                "Zalecone leczenie",
                "Zastosowane leczenie",
                "Leczenie",
                "Terapia",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.treatment_result",
            labels: &["Результат лечения", "Исход лечения", "Эффект лечения"],
            multiline: true,
        },
        LabelRule {
            field: "medical.discharge_condition",
            labels: &["Состояние при выписке", "Состояние на момент выписки"],
            multiline: true,
        },
        LabelRule {
            field: "medical.recommendations",
            labels: &["Рекомендации", "Рекомендовано", "Zalecenia"],
            multiline: true,
        },
        LabelRule {
            field: "medical.labs",
            labels: &[
                "Лабораторные исследования",
                "Лабораторные данные",
                "Результаты анализов",
                "Результаты обследований",
                "Результаты исследований",
                "Анализы",
                "Wyniki badań",
                "Wyniki badan",
            ],
            multiline: true,
        },
        LabelRule {
            field: "medical.labs_date",
            labels: &["Дата анализов", "Дата лабораторных исследований"],
            multiline: false,
        },
        LabelRule {
            field: "medical.workplace",
            labels: &[
                "Работает в организации",
                "Место работы",
                "Работа",
                "Miejsce pracy",
                "Zakład pracy",
                "Zaklad pracy",
            ],
            multiline: false,
        },
        LabelRule {
            field: "medical.position",
            labels: &["Должность", "Stanowisko", "Zawód", "Zawod"],
            multiline: false,
        },
        LabelRule {
            field: "medical.sick_leave_number",
            labels: &["Номер больничного", "Больничный лист №", "Лист нетрудоспособности №"],
            multiline: false,
        },
        LabelRule {
            field: "medical.attending_doctor",
            labels: &["Лечащий врач", "Lekarz prowadzący", "Lekarz prowadzacy"],
            multiline: false,
        },
        LabelRule {
            field: "medical.department_head",
            labels: &[
                "Заведующий отделением",
                "Зав. отделением",
                "Зав. отд.",
                "Ordynator",
                "Kierownik oddziału",
                "Kierownik oddzialu",
            ],
            multiline: false,
        },
    ]
}
'''
          source = source[:rules_start] + medical_rules + source[rules_end:]

          clean_start = source.index("fn clean_inline_value(value: &str) -> String {")
          clean_end = source.index("\nfn first_date_candidate", clean_start)
          compact_helper = r'''fn clean_inline_value(value: &str) -> String {
    let mut end = value.len();
    for (index, ch) in value.char_indices() {
        if !matches!(ch, ',' | ';' | '.') {
            continue;
        }
        let tail = value[index + ch.len_utf8()..].trim_start();
        if !tail.is_empty() && looks_like_known_label(tail) {
            end = index;
            break;
        }
    }
    if let Some(next_label) = next_explicit_inline_label_start(value) {
        end = end.min(next_label);
    }
    clean_value(&value[..end])
}

fn next_explicit_inline_label_start(value: &str) -> Option<usize> {
    let mut best: Option<usize> = None;
    for rule in generic_rules().into_iter().chain(medical_rules()) {
        for label in rule.labels {
            let Some(label_end) = find_label_end(value, label) else {
                continue;
            };
            let Some(label_start) = label_start_from_end(value, label, label_end) else {
                continue;
            };
            if label_start == 0 {
                continue;
            }
            let tail = value[label_end..].trim_start();
            let explicit_separator = tail.chars().next().is_some_and(|ch| {
                matches!(ch, ':' | '№' | '#' | '-' | '—' | '–')
            });
            if !explicit_separator {
                continue;
            }
            best = Some(best.map_or(label_start, |current| current.min(label_start)));
        }
    }
    best
}

fn label_start_from_end(value: &str, label: &str, mut end: usize) -> Option<usize> {
    if !value.is_char_boundary(end) {
        return None;
    }
    for _ in label.chars() {
        end = value[..end].char_indices().next_back()?.0;
    }
    Some(end)
}
'''
          source = source[:clean_start] + compact_helper + source[clean_end:]

          test_anchor = '''    #[test]\n    fn medical_values_are_mirrored_into_generic_core_fields() {'''
          donor_unit_tests = r'''    #[test]
    fn donor_medical_compact_primary_keeps_each_value_inside_its_field() {
        let text = "Первичный осмотр 05.06.2026 История болезни № 12345 Ф.И.О.: Иванов Иван Иванович Возраст: 45 лет Место жительства: г. Нижний Новгород, ул. Пушкина, 1 Место работы: ООО Ромашка Должность: инженер Жалобы: головная боль Анамнез заболевания: заболел вчера Анамнез жизни: рос и развивался нормально Соматический статус: стабилен Профильный статус: без отрицательной динамики План обследования: ОАК Лечение: режим, терапия Диагноз: J20 Острый бронхит";
        let (case, _) = parse_source_text(text, 2026);
        assert_eq!(case.get("medical.case_number"), Some("12345"));
        assert_eq!(case.get("subject.name"), Some("Иванов Иван Иванович"));
        assert_eq!(case.get("subject.age"), Some("45 лет"));
        assert_eq!(case.get("medical.workplace"), Some("ООО Ромашка"));
        assert_eq!(case.get("medical.position"), Some("инженер"));
        assert_eq!(case.get("medical.complaints"), Some("головная боль"));
        assert_eq!(case.get("medical.anamnesis_disease"), Some("заболел вчера"));
        assert_eq!(case.get("medical.anamnesis_life"), Some("рос и развивался нормально"));
        assert_eq!(case.get("medical.somatic_status"), Some("стабилен"));
        assert_eq!(case.get("medical.profile_status"), Some("без отрицательной динамики"));
        assert_eq!(case.get("medical.examination_plan"), Some("ОАК"));
        assert_eq!(case.get("medical.treatment"), Some("режим, терапия"));
        assert_eq!(case.get("medical.diagnosis"), Some("J20 Острый бронхит"));
    }

    #[test]
    fn donor_medical_blocks_stop_at_the_next_clinical_heading() {
        let text = "ПЕРВИЧНЫЙ ОСМОТР\nИстория болезни № 77\nЖалобы:\nтревога и бессонница\nАнамнез заболевания:\nухудшение в течение недели\nАнамнез жизни:\nбез особенностей\nСоматический статус:\nстабилен\nПрофильный статус:\nориентирован\nЛабораторные исследования:\nОАК от 01.06.2026: Hb 140 г/л\nЛечение:\nрежим, терапия\nДиагноз:\nF41.1 Генерализованное тревожное расстройство";
        let (case, _) = parse_source_text(text, 2026);
        assert_eq!(case.get("medical.complaints"), Some("тревога и бессонница"));
        assert_eq!(case.get("medical.anamnesis_disease"), Some("ухудшение в течение недели"));
        assert_eq!(case.get("medical.anamnesis_life"), Some("без особенностей"));
        assert_eq!(case.get("medical.somatic_status"), Some("стабилен"));
        assert_eq!(case.get("medical.profile_status"), Some("ориентирован"));
        assert_eq!(case.get("medical.labs"), Some("ОАК от 01.06.2026: Hb 140 г/л"));
        assert_eq!(case.get("medical.treatment"), Some("режим, терапия"));
        assert_eq!(case.get("medical.diagnosis"), Some("F41.1 Генерализованное тревожное расстройство"));
    }

    #[test]
    fn donor_medical_parser_never_invents_labs_when_source_has_none() {
        let text = "ПЕРВИЧНЫЙ ОСМОТР\nИстория болезни № 77\nДиагноз: J20 Острый бронхит\nЛечение: режим";
        let (case, _) = parse_source_text(text, 2026);
        assert_eq!(case.get("medical.labs"), None);
        assert_eq!(case.get("medical.labs_date"), None);
    }

'''
          if test_anchor not in source:
              raise SystemExit("source_parser unit test anchor missing")
          source = source.replace(test_anchor, donor_unit_tests + test_anchor, 1)
          source_path.write_text(source, encoding="utf-8")

          # 2) Make historical donor placeholders resolve to one canonical schema.
          aliases_path = Path("crates/dokkomplekt-core/src/field_aliases.rs")
          aliases = aliases_path.read_text(encoding="utf-8")
          alias_anchor = '        "person.address" | "patient.address" => "subject.address".into(),\n'
          alias_insert = alias_anchor + '''        "person.age" | "patient.age" => "subject.age".into(),
        "complaints" | "medical.complaints_text" => "medical.complaints".into(),
        "anamnesis.disease" | "disease_anamnesis" => "medical.anamnesis_disease".into(),
        "anamnesis.life" | "life_anamnesis" => "medical.anamnesis_life".into(),
        "status.objective" | "status.somatic" | "somatic_status" => "medical.somatic_status".into(),
        "status.profile" | "status.mental" | "mental_status" => "medical.profile_status".into(),
        "examination.plan" | "examination_plan" => "medical.examination_plan".into(),
        "treatment.result" => "medical.treatment_result".into(),
        "condition.discharge" => "medical.discharge_condition".into(),
        "labs.results" | "labs.block" | "analysis.results" | "analyses.results" => "medical.labs".into(),
        "labs.date" => "medical.labs_date".into(),
        "labs.source" => "medical.labs_source".into(),
        "labs.date_policy" => "medical.labs_date_policy".into(),
'''
          if aliases.count(alias_anchor) != 1:
              raise SystemExit("field_aliases canonical anchor mismatch")
          aliases = aliases.replace(alias_anchor, alias_insert, 1)
          storage_anchor = '        "subject.address" => &["subject.address", "person.address", "patient.address"],\n'
          storage_insert = storage_anchor + '''        "subject.age" => &["subject.age", "person.age", "patient.age"],
        "medical.complaints" => &["medical.complaints", "complaints", "medical.complaints_text"],
        "medical.anamnesis_disease" => &["medical.anamnesis_disease", "anamnesis.disease", "disease_anamnesis"],
        "medical.anamnesis_life" => &["medical.anamnesis_life", "anamnesis.life", "life_anamnesis"],
        "medical.somatic_status" => &["medical.somatic_status", "status.objective", "status.somatic", "somatic_status"],
        "medical.profile_status" => &["medical.profile_status", "status.profile", "status.mental", "mental_status"],
        "medical.examination_plan" => &["medical.examination_plan", "examination.plan", "examination_plan"],
        "medical.treatment_result" => &["medical.treatment_result", "treatment.result"],
        "medical.discharge_condition" => &["medical.discharge_condition", "condition.discharge"],
        "medical.labs" => &["medical.labs", "labs.results", "labs.block", "analysis.results", "analyses.results"],
        "medical.labs_date" => &["medical.labs_date", "labs.date"],
        "medical.labs_source" => &["medical.labs_source", "labs.source"],
        "medical.labs_date_policy" => &["medical.labs_date_policy", "labs.date_policy"],
'''
          if aliases.count(storage_anchor) != 1:
              raise SystemExit("field_aliases storage anchor mismatch")
          aliases = aliases.replace(storage_anchor, storage_insert, 1)
          aliases_path.write_text(aliases, encoding="utf-8")

          # 3) Register the medical semantics so old and new user templates map cleanly.
          registry_path = Path("crates/dokkomplekt-core/src/field_registry.rs")
          registry = registry_path.read_text(encoding="utf-8")
          subject_marker = '        field(\n            "subject.address",\n'
          subject_age = '''        field(
            "subject.age",
            "Возраст",
            DomainKind::Generic,
            false,
            &["age", "Возраст", "patient.age", "person.age"],
        ),
'''
          if registry.count(subject_marker) < 1:
              raise SystemExit("field_registry subject.address marker missing")
          registry = registry.replace(subject_marker, subject_age + subject_marker, 1)

          medical_marker = '        field(\n            "medical.diary_schedule_style",\n'
          clinical_fields = '''        field(
            "medical.complaints",
            "Жалобы",
            DomainKind::Medical,
            false,
            &["complaints", "Жалобы", "Жалобы при поступлении"],
        ),
        field(
            "medical.anamnesis_disease",
            "Анамнез заболевания",
            DomainKind::Medical,
            false,
            &["anamnesis.disease", "disease_anamnesis", "Анамнез заболевания"],
        ),
        field(
            "medical.anamnesis_life",
            "Анамнез жизни",
            DomainKind::Medical,
            false,
            &["anamnesis.life", "life_anamnesis", "Анамнез жизни"],
        ),
        field(
            "medical.profile_status",
            "Профильный статус",
            DomainKind::Medical,
            false,
            &["status.profile", "status.mental", "mental_status", "Профильный статус", "Психический статус"],
        ),
        field(
            "medical.somatic_status",
            "Соматический / объективный статус",
            DomainKind::Medical,
            false,
            &["status.objective", "status.somatic", "somatic_status", "Соматический статус", "Объективный статус"],
        ),
        field(
            "medical.examination_plan",
            "План обследования",
            DomainKind::Medical,
            false,
            &["examination.plan", "examination_plan", "План обследования"],
        ),
        field(
            "medical.treatment_result",
            "Результат лечения",
            DomainKind::Medical,
            false,
            &["treatment.result", "Результат лечения", "Исход лечения"],
        ),
        field(
            "medical.labs_source",
            "Источник результатов исследований",
            DomainKind::Medical,
            false,
            &["labs.source", "Источник анализов"],
        ),
        field(
            "medical.labs_date_policy",
            "Политика даты исследований",
            DomainKind::Medical,
            false,
            &["labs.date_policy", "Политика даты анализов"],
        ),
        field(
            "medical.labs_without",
            "Без лабораторных исследований",
            DomainKind::Medical,
            false,
            &["labs.without", "Без анализов"],
        ),
'''
          if registry.count(medical_marker) != 1:
              raise SystemExit("field_registry medical diary marker mismatch")
          registry = registry.replace(medical_marker, clinical_fields + medical_marker, 1)
          registry = registry.replace(
              '&[\"discharge.condition\",\n                \"dischargeCondition\",\n                \"Состояние при выписке\",\n            ],',
              '&[\"discharge.condition\",\n                \"condition.discharge\",\n                \"dischargeCondition\",\n                \"Состояние при выписке\",\n            ],',
              1,
          )
          registry = registry.replace(
              '&[\n                \"labs.results\",\n                \"labResults\",\n                \"Лабораторные исследования\",\n                \"Анализы\",\n            ],',
              '&[\n                \"labs.results\",\n                \"labs.block\",\n                \"analysis.results\",\n                \"analyses.results\",\n                \"labResults\",\n                \"Результаты анализов\",\n                \"Лабораторные исследования\",\n                \"Анализы\",\n            ],',
              1,
          )
          registry_path.write_text(registry, encoding="utf-8")

          # 4) Expand the medical domain's deterministic field contract (still no medical logic in UI).
          medical_path = Path("crates/dokkomplekt-core/src/domains/medical.rs")
          medical = medical_path.read_text(encoding="utf-8")
          fields_start = medical.index("        field_rules: vec![")
          fields_end = medical.index("        ],\n    }", fields_start)
          field_rules = '''        field_rules: vec![
            FieldExtractionRule { field_id: "medical.case_number".into(), aliases: vec!["Номер истории болезни".into(), "История болезни №".into()], required: true },
            FieldExtractionRule { field_id: "subject.name".into(), aliases: vec!["ФИО пациента".into(), "Ф.И.О.".into()], required: false },
            FieldExtractionRule { field_id: "subject.age".into(), aliases: vec!["Возраст".into()], required: false },
            FieldExtractionRule { field_id: "medical.diagnosis".into(), aliases: vec!["Диагноз".into(), "Клинический диагноз".into()], required: true },
            FieldExtractionRule { field_id: "medical.treatment".into(), aliases: vec!["Лечение".into(), "Назначенное лечение".into()], required: false },
            FieldExtractionRule { field_id: "medical.complaints".into(), aliases: vec!["Жалобы".into()], required: false },
            FieldExtractionRule { field_id: "medical.anamnesis_disease".into(), aliases: vec!["Анамнез заболевания".into()], required: false },
            FieldExtractionRule { field_id: "medical.anamnesis_life".into(), aliases: vec!["Анамнез жизни".into()], required: false },
            FieldExtractionRule { field_id: "medical.somatic_status".into(), aliases: vec!["Соматический статус".into(), "Объективный статус".into()], required: false },
            FieldExtractionRule { field_id: "medical.profile_status".into(), aliases: vec!["Профильный статус".into(), "Психический статус".into()], required: false },
            FieldExtractionRule { field_id: "medical.examination_plan".into(), aliases: vec!["План обследования".into()], required: false },
            FieldExtractionRule { field_id: "medical.labs".into(), aliases: vec!["Лабораторные исследования".into(), "Анализы".into()], required: false },
            FieldExtractionRule { field_id: "medical.recommendations".into(), aliases: vec!["Рекомендации".into()], required: false },
            FieldExtractionRule { field_id: "medical.attending_doctor".into(), aliases: vec!["Лечащий врач".into()], required: false },
            FieldExtractionRule { field_id: "medical.department_head".into(), aliases: vec!["Заведующий отделением".into(), "Зав. отделением".into()], required: false },
'''
          medical = medical[:fields_start] + field_rules + medical[fields_end:]
          medical_path.write_text(medical, encoding="utf-8")

          # 5) Lock the donor behavior as public integration tests, not only implementation-local tests.
          test_path = Path("crates/dokkomplekt-core/tests/donor_medical_source_parity.rs")
          test_path.write_text(r'''use dokkomplekt_core::{canonical_field_id_for_domain, parse_source_text, DomainKind};

#[test]
fn donor_compact_primary_document_populates_clinical_case_without_field_bleed() {
    let text = "Первичный осмотр 05.06.2026 История болезни № 12345 Ф.И.О.: Иванов Иван Иванович Возраст: 45 лет Место жительства: г. Нижний Новгород, ул. Пушкина, 1 Место работы: ООО Ромашка Должность: инженер Жалобы: головная боль Анамнез заболевания: заболел вчера Анамнез жизни: рос и развивался нормально Соматический статус: стабилен Профильный статус: ориентирован План обследования: ОАК Лечение: режим, терапия Диагноз: J20 Острый бронхит";
    let (case, report) = parse_source_text(text, 2026);
    assert_eq!(case.get("medical.admission_date"), Some("05.06.2026"));
    assert_eq!(case.get("medical.case_number"), Some("12345"));
    assert_eq!(case.get("subject.name"), Some("Иванов Иван Иванович"));
    assert_eq!(case.get("subject.age"), Some("45 лет"));
    assert_eq!(case.get("medical.workplace"), Some("ООО Ромашка"));
    assert_eq!(case.get("medical.position"), Some("инженер"));
    assert_eq!(case.get("medical.complaints"), Some("головная боль"));
    assert_eq!(case.get("medical.anamnesis_disease"), Some("заболел вчера"));
    assert_eq!(case.get("medical.anamnesis_life"), Some("рос и развивался нормально"));
    assert_eq!(case.get("medical.somatic_status"), Some("стабилен"));
    assert_eq!(case.get("medical.profile_status"), Some("ориентирован"));
    assert_eq!(case.get("medical.examination_plan"), Some("ОАК"));
    assert_eq!(case.get("medical.treatment"), Some("режим, терапия"));
    assert_eq!(case.get("medical.diagnosis"), Some("J20 Острый бронхит"));
    assert!(report.warnings.iter().all(|warning| !warning.contains("похож на ФИО")));
}

#[test]
fn donor_historical_template_placeholders_resolve_to_current_medical_schema() {
    let medical = Some(&DomainKind::Medical);
    for (legacy, canonical) in [
        ("patient.age", "subject.age"),
        ("complaints", "medical.complaints"),
        ("anamnesis.disease", "medical.anamnesis_disease"),
        ("anamnesis.life", "medical.anamnesis_life"),
        ("status.objective", "medical.somatic_status"),
        ("status.profile", "medical.profile_status"),
        ("examination.plan", "medical.examination_plan"),
        ("treatment.result", "medical.treatment_result"),
        ("condition.discharge", "medical.discharge_condition"),
        ("labs.results", "medical.labs"),
        ("labs.date", "medical.labs_date"),
    ] {
        assert_eq!(
            canonical_field_id_for_domain(legacy, medical).as_deref(),
            Some(canonical),
            "legacy placeholder {legacy}"
        );
    }
}

#[test]
fn donor_labs_contract_is_explicit_and_never_synthesizes_results() {
    let with_labs = "ПЕРВИЧНЫЙ ОСМОТР\nИстория болезни № 77\nЛабораторные исследования:\nОАК от 01.06.2026: Hb 140 г/л\nДиагноз: J20 Острый бронхит";
    let (case, _) = parse_source_text(with_labs, 2026);
    assert_eq!(case.get("medical.labs"), Some("ОАК от 01.06.2026: Hb 140 г/л"));

    let without_labs = "ПЕРВИЧНЫЙ ОСМОТР\nИстория болезни № 78\nДиагноз: J20 Острый бронхит\nЛечение: режим";
    let (case, _) = parse_source_text(without_labs, 2026);
    assert_eq!(case.get("medical.labs"), None);
    assert_eq!(case.get("medical.labs_date"), None);
}
''', encoding="utf-8")

          # 6) Make the migration inventory honest: this donor module was previously omitted.
          inventory_path = Path("docs/LEGACY_MIGRATION_INVENTORY.json")
          inventory = json.loads(inventory_path.read_text(encoding="utf-8"))
          donor = next(item for item in inventory["donors"] if item["repository"] == "mailsvb2-bot/Dokkomplekt")
          if not any(entry["path"] == "medical_renderer_labs.py" for entry in donor["entries"]):
              donor["entries"].append({
                  "path": "medical_renderer_labs.py",
                  "status": "migrated-domain-profile",
                  "targets": [
                      "crates/dokkomplekt-core/src/source_parser.rs",
                      "crates/dokkomplekt-core/src/field_registry.rs",
                      "crates/dokkomplekt-core/src/field_aliases.rs",
                  ],
                  "note": "Explicit laboratory results/date/source semantics are migrated without synthetic medical-result generation.",
              })
              donor["entries"].sort(key=lambda entry: entry["path"])
          inventory_path.write_text(json.dumps(inventory, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
          PY

          cargo fmt --all
          cargo test --locked -p dokkomplekt-core --test donor_medical_source_parity
          cargo test --locked -p dokkomplekt-core source_parser::tests
          python scripts/check_legacy_migration_inventory.py
          python scripts/static_quality_gate.py --source-only

      - name: Commit canonical implementation and remove one-time workflow
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/workflows/one-time-donor-medical-source-parity.yml
          git config user.name "BusinesMetro"
          git config user.email "mailsvb2@gmail.com"
          git add crates/dokkomplekt-core/src/source_parser.rs \
                  crates/dokkomplekt-core/src/field_aliases.rs \
                  crates/dokkomplekt-core/src/field_registry.rs \
                  crates/dokkomplekt-core/src/domains/medical.rs \
                  crates/dokkomplekt-core/tests/donor_medical_source_parity.rs \
                  docs/LEGACY_MIGRATION_INVENTORY.json \
                  .github/workflows/one-time-donor-medical-source-parity.yml
          git commit -m "Restore donor medical source semantics"
          git push origin HEAD:agent/donor-medical-source-parity-20260817
